# Postgres em produção; SQLite via Prisma para dev/teste rápido
DATABASE_URL="file:./prisma/dev.db"

# NextAuth / Auth.js
AUTH_SECRET="troque-em-producao-gerando-com-openssl-rand-base64-32"
AUTH_TRUST_HOST="true"

# Storage de logos e cards visuais (opcional, fallback local em dev)
STORAGE_PROVIDER="local"
SUPABASE_URL=""
SUPABASE_SERVICE_KEY=""
